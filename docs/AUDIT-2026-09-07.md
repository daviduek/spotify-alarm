# Auditoría 2026-09-07 — estado de cada hallazgo

Tres auditorías independientes (web Next 16, dominio + SQL, móvil Expo/nativo). Esta tabla es la
fuente de verdad de qué se corrigió en el commit `audit: …` y qué queda abierto.

Leyenda: ✅ corregido · 🟡 mitigado/parcial · ⏳ pendiente (requiere acción de David o fase posterior)

## Web (`apps/web`)

| Sev | Hallazgo | Estado | Dónde |
|---|---|---|---|
| **Crítico** | Clock mode nunca disparaba: al vencer el timer recomputaba "próxima" (ya mañana) y comparaba contra eso | ✅ | `lib/engine/alarmClock.ts` — captura el target, heartbeat 1 s, ventana de tardanza 10 min, re-chequeo al volver a visible |
| Alto | URL firmada de grabación (1 h) persistida en el plan → alarma muda al día siguiente | ✅ | `SourcePicker` ya no guarda `fileUri`; `ClockMode` re-firma al disparar; `webAudio.crossfadeToRecording` rechaza si no reproduce y NO apaga el fallback |
| Alto | Refresh concurrente de Spotify borraba la conexión (rotación de refresh token) | ✅ | `lib/spotify/server.ts` — update optimista por `refresh_token`, re-lectura antes de borrar |
| Alto | Con `SPOTIFY_CLIENT_SECRET` no se enviaba `code_verifier` → intercambio fallaba | ✅ | `exchangeCode` envía verifier siempre |
| Alto | iOS Safari bloquea el audio creado fuera del gesto | ✅ | `webAudio.unlock()` prepara un pool de 2 `<audio>` dentro del tap "Arm"; se reutilizan al sonar |
| Alto | Sin recuperación de contraseña | ✅ | `/auth/reset`, `/auth/update-password`, acciones `requestPasswordReset`/`updatePassword`, link en login |
| Alto | Tokens de Spotify legibles desde el browser (`for all` en RLS) | 🟡 | Código: lectura/escritura solo con service role (`lib/supabase/admin.ts`). **DB: aplicar `supabase/migrations/0002_hardening.sql`** (ver HANDOFF) |
| Medio | Open redirect en `?next=//evil` | ✅ | `safeNext()` en acciones y callback |
| Medio | Alarmas "once" sonaban todos los días | ✅ | Se deshabilitan al sonar |
| Medio | Hold-to-stop con doble timer (mouse+touch) | ✅ | Pointer events + guard |
| Medio | Fallback apagado antes de que Spotify realmente suene | ✅ | `player.confirmPlaying()` (estado SDK o `/me/player`) antes del fade-out |
| Medio | Snooze en `setState` (doble disparo en StrictMode), sin limpiar | ✅ | ref + cleanup en disarm/unmount |
| Medio | `/api/spotify/*` sin sesión redirigía a HTML | ✅ | JSON 401 en `session.ts` |
| Medio | Errores tragados sin log | ✅ | `console.error` en callback/token/recordings |
| Medio | Link de mail abierto en otro dispositivo fallaba (PKCE) | ✅ | Callback soporta `token_hash`+`type`; mensaje amigable |
| Medio | Preset de fade se perdía al editar | ✅ | `fadePresetFor()` en el editor |
| Medio | `zod` no declarado; `@wake/domain` no declarado | ✅ | `apps/web/package.json` |
| Medio | MIME con `;codecs=` rechazado por el bucket; objeto huérfano si falla el insert | ✅ | `recordings.ts` |
| Medio | Pestaña oculta sin aviso; sin PWA | ✅ | Aviso al ocultar; `manifest.ts`, iconos, `display: standalone` |
| Bajo | Metadata/OG/robots/sitemap/error/not-found | ✅ | `layout.tsx`, `opengraph-image.tsx`, `robots.ts`, `sitemap.ts`, `error.tsx`, `not-found.tsx` |
| Bajo | `decodeURIComponent` en `?error=%` → 500 | ✅ | login page |
| Bajo | Layout raíz llamaba `cookies()` → todo dinámico | ✅ | nav de sesión en `<Suspense>` |
| Bajo | CSP | ⏳ | Empezar con `Content-Security-Policy-Report-Only` (sdk.scdn.co, api.spotify.com, *.supabase.co) |
| Bajo | `profiles.time_zone` nunca se escribe | ⏳ | Persistir `Intl.DateTimeFormat().resolvedOptions().timeZone` al login (útil para móvil) |
| Bajo | Reconciliación offline (IndexedDB) | ⏳ | Fase posterior |

## Dominio + SQL (`packages/domain`, `supabase/migrations`)

| Sev | Hallazgo | Estado |
|---|---|---|
| Alto | Readiness decía "ready" con provider no verificado / grabación desconocida | ✅ `provider_unknown`, `recordingAvailable !== true` |
| Medio | `alarm_history` insertaba 2-3 filas por sonido, sin id compartido | ✅ id por ocurrencia + upsert progresivo (web) |
| Medio | Filas que la DB acepta y zod rechaza desaparecían en silencio | ✅ checks en SQL (0002) + `console.warn` en `rowToAlarm` |
| Medio | `weekdays` duplicados pasaban | ✅ refine |
| Bajo | `hour: NaN` → `Invalid Date` → loop de `setTimeout(NaN)` | ✅ guards en `nextOccurrence` |
| Bajo | Curva desconocida / duración NaN → volumen 0 (alarma muda) | ✅ `default: linear`, guards, schedule sin drift |
| Bajo | Policies `for all` en history/profiles; sin FK `alarm_id`; sin checks | 🟡 en `0002_hardening.sql` (pendiente de aplicar) |
| Bajo | Objetos de storage huérfanos al borrar usuario | ⏳ sweep programado (no borrar `storage.objects` a mano) |
| Bajo | Tests faltantes (DST, límites, NaN, monotonicidad) | 🟡 `test/hardening.test.ts` (+11). DST real requiere `TZ` fijo en vitest; pendiente |

## Móvil (`apps/mobile`)

| Sev | Hallazgo | Estado |
|---|---|---|
| **Bloqueante build** | `SCHEDULE_EXACT_ALARM` declarado en `app.config.ts` sin `maxSdkVersion` y en el manifest del módulo con `maxSdkVersion=32` → manifest merger falla | ✅ permisos de alarma solo en el manifest del módulo |
| Alto | Android 13+: `POST_NOTIFICATIONS` nunca se pedía → sin notificación de FGS → sin pantalla de alarma | ✅ `PermissionsAndroid.request` en `requestPermission()` |
| Alto | Android 11+: `canOpenURL('spotify:')` siempre false sin `<queries>` | ✅ `<queries>` en el manifest del módulo |
| Alto | iOS < 26 instala pero no puede sonar (sin fallback) | ✅ `deploymentTarget` 26.0 (se rechaza la instalación en vez de fallar en silencio). Fallback real (critical alerts + background audio) = Fase 1 |
| Medio | Receiver de eventos solo en `OnCreate` (reactContext puede ser null) | ✅ también en `OnStartObserving` |
| Bajo | `TIME_SET` en BootReceiver (nunca llega en API 26+) | ✅ removido |
| Bajo | `channel` en eas.json sin `expo-updates` | ✅ removido |
| Bajo | Imagen EAS `latest` sin pin | ⏳ pinear tras el primer build exitoso |
| Bajo | AlarmKit countdown sin Live Activity | ⏳ Fase 1 (`@bacons/apple-targets`) |
| — | Expo Go no viable (módulo nativo) | Confirmado: solo dev client / EAS |
