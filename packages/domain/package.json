{
  "name": "@wake/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Wake domain model: alarms, audio plans, fade engine, scheduling math, readiness. Pure TypeScript, no platform code.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^3.2.4"
  }
}
